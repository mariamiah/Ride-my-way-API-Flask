language: python
python:
  - "3.6.5"

#command to install dependencies

install:
- pip install -r requirements.txt



#command to run tests

script: python -m pytest && coverage run -m pytest && coverage report


after_success: npm run coverage
